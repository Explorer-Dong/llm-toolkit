[project]
name = "llm-evaluation"
version = "0.0.1"
description = "LLM evalution toolkit."
requires-python = ">=3.12"
dependencies = [
    "openai",
    "datasets",
    "pydantic",
    "tqdm",
    "rich",
]

[tool.uv]
package = false
